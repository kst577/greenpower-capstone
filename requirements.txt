# Runnable pipeline (portable): standard scientific Python only.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
# Optional — production storage / interactive dashboard / deep model
# psycopg2-binary>=2.9      # TimescaleDB connection
# statsmodels>=0.14         # SARIMA
# tensorflow>=2.13          # production LSTM (models.train_lstm_keras)
# streamlit>=1.30           # interactive dashboard (dashboard/app.py)
# plotly>=5.18
# pyarrow>=14               # parquet exports
